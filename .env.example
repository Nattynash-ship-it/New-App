# --- AI (Reverse Recipe Engine + Natural Language Quick Add) ---
# Optional. Without a key the app falls back to a deterministic on-device
# engine, so everything still works — the AI just gets smarter with a key.
ANTHROPIC_API_KEY=

# --- Grocery delivery integrations (placeholders) ---
# Webhook endpoints for routing generated grocery lists to delivery services.
WHOLE_FOODS_WEBHOOK_URL=
ALDI_WEBHOOK_URL=

# --- Future: Supabase backend (see supabase/schema.sql) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
